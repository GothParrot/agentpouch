services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: agentpouch
      POSTGRES_USER: agentpouch
      POSTGRES_PASSWORD: agentpouch
    ports:
      - "5432:5432"
    tmpfs:
      - /var/lib/postgresql/data
